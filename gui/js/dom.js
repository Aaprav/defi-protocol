
const DOM_THEME_TOGGLE = document.getElementById("theme-toggle-input");

const DOM_CONNECT_WALLET = document.getElementById("connectWallet");
const DOM_SWAP_BTN = document.getElementById("swapBtn");

const DOM_NOTIFICATIONS = document.querySelector(".notifications");

const DOM_TOGGLE_INPUTS = document.querySelector(".swap_input_toggle");

const DOM_SLIPPAGE_INPUT = document.getElementById("input-slippage");
const DOM_DEADLINE_INPUT = document.getElementById("input-deadline");

const DOM_SWAP_INPUT = document.getElementById("swap-input");
const DOM_BALANCE = DOM_SWAP_INPUT.children[0].children[1].children[0];
const DOM_INPUT_VALUE = DOM_SWAP_INPUT.children[1].children[0];
const DOM_INPUT_SWITCH = DOM_SWAP_INPUT.children[1].children[1];
const DOM_INPUT_TEXT = DOM_SWAP_INPUT.children[2];

const DOM_SWAP_OUTPUT = document.getElementById("swap-output");
const DOM_OUTPUT_VALUE = DOM_SWAP_OUTPUT.children[1].children[0];
const DOM_OUTPUT_SWITCH = DOM_SWAP_OUTPUT.children[1].children[1];
const DOM_OUTPUT_TEXT = DOM_SWAP_OUTPUT.children[2];

const DOM_POPUP = document.querySelector(".popup");
const DOM_POPUP_CLOSE = DOM_POPUP.children[0].children[0].children[1];

const DOM_TOKEN_INPUT = document.getElementById("token-input");


const DOM_SWAP_INFO = document.querySelector(".swap_info");
const DOM_SWAP_INFO_TOGGLE = DOM_SWAP_INFO.children[0].children[1];
